# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# supabase CLI local state (contains linked project ref, pooler URL, local secrets)
supabase/.temp
supabase/.branches

# typescript
*.tsbuildinfo
next-env.d.ts

# pdf.js worker — คัดลอกอัตโนมัติจาก node_modules ทุกครั้งหลัง npm install
# (ดู scripts/copy-pdf-worker.js) ไม่ต้อง commit เข้า Git
/public/pdf.worker.min.mjs

# editor
.vscode/*
!.vscode/extensions.json
